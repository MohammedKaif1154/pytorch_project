name: envpytorch
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly
  - pip
  - pip:
      - numpy
      - pandas
      - matplotlib
      - scikit-learn
      - jupyter
  